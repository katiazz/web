<!DOCTYPE html>
<html>
<head>
  <title>Desencriptar Texto</title>
</head>
<body>
  <h2>Desencriptar Texto</h2>

  <!-- Formulario de desencriptación -->
  <form method="POST" action="/decrypt">
    <label>Texto encriptado:</label><br>
    <input type="text" name="encryptedText" required><br>
    <label>IV:</label><br>
    <input type="text" name="iv" required><br>
    <button type="submit">Desencriptar</button>
  </form>

  <% if (result) { %>
    <h3>Texto desencriptado:</h3>
    <p><%= result %></p>
  <% } %>
  <% if (error) { %><p style="color:red;"><%= error %></p><% } %>

  <p><a href="/encrypt">Volver a Encriptar</a></p>

  <!-- Enlace para cerrar sesión -->
  <p><a href="/logout">Cerrar sesión</a></p>
</body>
</html>


